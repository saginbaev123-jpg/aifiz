# AI Physics KZ — Railway-ға орналастыру

## 1. GitHub-қа жүктеу

Жоба бумасын GitHub репозиторийіне салыңыз. `.gitignore` ішіндегі `.env`, деректер базасы және жүктелген жеке файлдар GitHub-қа жіберілмеуі тиіс.

## 2. Railway жобасын құру

1. Railway-ға кіріңіз.
2. `New Project` батырмасын басыңыз.
3. `Deploy from GitHub repo` таңдаңыз.
4. AI Physics KZ репозиторийін таңдаңыз.

Жоба түбіріндегі `Dockerfile` және `railway.json` автоматты қолданылады. Сервер Railway берген `PORT` айнымалысын өзі қабылдайды.

## 3. Құпия айнымалылар

Railway сервисінің `Variables` бөлімінде мыналарды қосыңыз:

```text
OPENAI_API_KEY=өзіңіздің_API_кілтіңіз
OPENAI_MODEL=gpt-5.6-luna
APP_MODE=production
ALLOW_USER_API_KEY=false
AI_PHYSICS_DB=/app/storage/ai_physics_kz.db
AI_PHYSICS_UPLOAD_DIR=/app/storage/uploads
AI_PHYSICS_GENERATED_DIR=/app/storage/generated_files
```

API кілтін кодқа, `.env` файлына немесе GitHub репозиторийіне салмаңыз.

## 4. Деректерді тұрақты сақтау

SQLite базасы, әр сұрақтың видеосы, тіркелген және ЖИ жасаған файлдар үшін бір Railway Volume жасаңыз:

```text
Mount path: /app/storage
```

Осы жерде `ai_physics_kz.db`, `uploads/` және `generated_files/` сақталады. Volume қосылмаса, жаңа deploy кезінде оқушы деректері мен бейнежазбалары жоғалуы мүмкін. Бір SQLite базасын қолданғанда сервистің бір ғана көшірмесін іске қосыңыз; деректердің резервтік көшірмесін бөлек жасаңыз.

## 5. Сайт адресін ашу

`Settings → Networking → Generate Domain` таңдаңыз. Railway берген адрес арқылы сайтты телефоннан және компьютерден тексеріңіз.

Тексерілетін негізгі әрекеттер:

- оқушы мен мұғалім тіркелуі;
- `7А`, `8Б`, `9Ә` сияқты сынып таңдау;
- мұғалімнің сынып құруы және оқушының кодпен қосылуы;
- ЖИ диагностика және тапсырма генерациясы;
- PISA Lab;
- ЖИ мұғалім;
- нәтиженің базаға сақталуы;
- Word және PDF есептерін жүктеу.

## 6. Кейін жаңарту

GitHub-қа жаңа өзгеріс жіберілген сайын Railway жаңа нұсқаны автоматты орналастырады. Дұрыс архитектура сақталғандықтан, кейін БЖБ/ТЖБ генераторы, ҰБТ дайындығы, рейтинг және ата-ана кабинетін бөлек модульдер ретінде қосуға болады.
